extends layout

block content
    body
        script.
            setTimeout(function() {
                window.location.href = '/dashboard';
            }, 2000);
        h2 Operation Successful!
        a(href='/') Ok!